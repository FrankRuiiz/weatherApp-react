var React = require('react');

function Home(props) {
    return (
        <div className="home" style={{ backgroundImage: "url('app/images/pattern.svg')" }}>
            <h1>Home page</h1>
        </div>
    );
}

module.exports = Home;